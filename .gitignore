/target
.env
*.key
server.log
.idea
logs

compose.override.yaml

/coverage
/fe/coverage
# Stray profiling data from instrumented coverage runs (cargo-llvm-cov)
*.profraw

# ts-rs default output (we use TS_RS_EXPORT_DIR instead)
**/bindings/*.ts

# Mac-specific
.DS_Store

docs/superpowers

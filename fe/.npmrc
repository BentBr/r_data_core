store-dir=.pnpm-store
# Don't prompt to remove node_modules when pnpm detects a major-version mismatch.
# The compose `node` service runs without a TTY and would deadlock on the prompt.
confirm-modules-purge=false